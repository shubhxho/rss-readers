# Social post kit

**Video:** `demo/rss-readers-4k.mp4` — 3840×2160, H.264/yuv420p, ~21s, `+faststart`.
Twitter/X-ready (drag-and-drop). Regenerate with `./demo/render-4k.sh`.

## Tweet copy

### Option A — build-in-public
> Built a terminal RSS reader in Go. ⚡
>
> • Charm Bubble Tea TUI + feed sidebar
> • concurrent fetch (errgroup)
> • 3-tier cache w/ ETag revalidation — warm start ~4ms vs 1.3s cold
> • OPML import/export, config you own
>
> MIT, on GitHub 👇

### Option B — punchy
> rss-readers: an aesthetic terminal RSS reader in Go + @charmcli Bubble Tea.
>
> Concurrent fetch, three-tier caching (warm launch in ~4ms), OPML, a feed
> sidebar, fuzzy search. All in the terminal.

### Option C — one-liner
> Terminal RSS, done right: Go + Bubble Tea, concurrent fetch, ETag-cached
> (warm start ~4ms), OPML in/out. 🦦

Attach the video, drop the repo link, tag `#golang` `#charmcli` `#TUI`.
